// import { Router, Request, Response } from 'express';

// const router = Router();

// // GET route
// router.get('/', (req: Request, res: Response) => {
//   res.send('Hello, World!');
// });

// // POST route
// router.post('/data', (req: Request, res: Response) => {
//   const { name, value } = req.body;
//   res.json({ message: 'Data received', data: { name, value } });
// });

// // PUT route
// router.put('/data/:id', (req: Request, res: Response) => {
//   const { id } = req.params;
//   const { name, value } = req.body;
//   res.json({ message: `Data with id ${id} updated`, data: { name, value } });
// });

// // DELETE route
// router.delete('/data/:id', (req: Request, res: Response) => {
//   const { id } = req.params;
//   res.json({ message: `Data with id ${id} deleted` });
// });

// export default router;
